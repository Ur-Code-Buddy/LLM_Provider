# Inference Console

Single-page client for the secured model API: **Workspace** for chat completions and **Administration** for key and usage management. Build with `npm run build` and serve the `dist/` output behind your gateway.
